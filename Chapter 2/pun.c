#include <stdio.h> /*include the standard IO library on compilation */

int main(void) /* Int, integer Main, main function of the program, Void, means there are no arguments to run this */
{
  printf("To C, or not to C: That is the question\n"); /* Print The Thing*/
  return 0; /* causes main to terminate, as it indicates that main=0, stops compilers throwing a hissy */
}
/* Name:pun.c
   Purpouse:prints a pun
   Author:Ada Serket
*/
